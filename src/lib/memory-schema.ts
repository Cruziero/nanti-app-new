import { z } from 'zod';
export const daySchema=z.string().regex(/^\d{4}-\d{2}-\d{2}$/).refine(s=>!Number.isNaN(Date.parse(s))&&new Date(s).toISOString().slice(0,10)===s,'Invalid date');
export const itemSchema=z.object({id:z.string().uuid(),title:z.string().trim().min(1).max(500),description:z.string().max(2000).optional(),kind:z.enum(['task','commitment','deadline','waiting','followup','invoice']),status:z.enum(['inbox','open','done','ignored','received']),priority:z.enum(['critical','high','medium','low']),due:daySchema.optional(),time:z.string().regex(/^([01]\d|2[0-3]):[0-5]\d$/).optional(),since:daySchema.optional(),personId:z.string().uuid().optional(),projectId:z.string().uuid().optional(),personName:z.string().max(200).optional(),projectName:z.string().max(200).optional(),source:z.string().max(200),sourceType:z.enum(['paste','screenshot','demo','manual','whatsapp','calendar']).optional(),quote:z.string().max(20000),aiNote:z.string().max(2000),confidence:z.number().min(0).max(1),createdBy:z.enum(['ai','user']),createdAt:z.string().optional(),reminderEnabled:z.boolean().optional(),reminderTime:z.string().datetime({offset:true}).optional(),reminderChannels:z.array(z.enum(['in_app','push','whatsapp','calendar'])).optional(),reminderIntensity:z.enum(['gentle','normal','persistent']).optional(),lastRemindedAt:z.string().optional(),reminderCount:z.number().optional()});
